#!/bin/sh
# skills/pmtx/SKILL.md is the canonical source of truth.
# Mirror it to .claude/skills/pmtx/SKILL.md before every commit.

CANONICAL="skills/pmtx/SKILL.md"
MIRROR=".claude/skills/pmtx/SKILL.md"

if [ -f "$CANONICAL" ]; then
    cp "$CANONICAL" "$MIRROR"
    git add "$MIRROR"
fi
